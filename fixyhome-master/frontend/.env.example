# Backend API URL
# In development, the Spring Boot backend runs on port 8080
# In production (Railway), set this to your backend service's public URL
NEXT_PUBLIC_API_URL=http://localhost:8080

# API request timeout in milliseconds
NEXT_PUBLIC_API_TIMEOUT=30000
